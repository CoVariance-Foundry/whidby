---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# speckit-m05-data-collection-xij Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-04-05

## Active Technologies
- Python 3.11+ + `pydantic>=2`, existing pipeline types/utilities, `src/config/constants.py` constants (`AIO_CTR_REDUCTION`, `INTENT_AIO_RATES`, `KNOWN_AGGREGATORS`) (002-m06-signal-extraction-rib)
- N/A (in-memory signal derivation from provided M5 payloads) (002-m06-signal-extraction-rib)
- Python 3.11+ + `anthropic` (existing), `networkx` (existing), `httpx` (existing), `pydantic>=2` (existing) (009-claude-native-agent)
- Filesystem (per-run artifacts in `research_runs/`), JSON graph (`research_graph.json`) (009-claude-native-agent)

- Python 3.11+ + `httpx`, `pydantic>=2`, existing `src/clients/dataforseo` client wrappers, `asyncio` (001-refresh-m5-data-collection)
- Python 3.11+ + Existing `LLMClient` (M3), `DataForSEOClient` (M0), `pydantic>=2`, `httpx`, shared constants in `src/config/constants.py` (001-keyword-expansion)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.11+: Follow standard conventions

## Recent Changes
- 009-claude-native-agent: Added Python 3.11+ + `anthropic` (existing), `networkx` (existing), `httpx` (existing), `pydantic>=2` (existing)
- 002-m06-signal-extraction-rib: Added Python 3.11+ + `pydantic>=2`, existing pipeline types/utilities, `src/config/constants.py` constants (`AIO_CTR_REDUCTION`, `INTENT_AIO_RATES`, `KNOWN_AGGREGATORS`)

- 001-refresh-m5-data-collection: Added Python 3.11+ + `httpx`, `pydantic>=2`, existing `src/clients/dataforseo` client wrappers, `asyncio`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
